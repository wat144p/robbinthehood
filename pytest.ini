[pytest]
testpaths = tests
pythonpath = .
addopts = -q --tb=short
filterwarnings =
    # bs4 passes a long-dead option to lxml's HTMLParser. Third-party noise;
    # silenced so that warnings from our own code stay visible.
    ignore:The 'strip_cdata' option:DeprecationWarning
